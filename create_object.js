/* Properties are qualities that define what makes up an object. 
  Note that similar objects share the same properties, but may have different values for those properties.*/

let duck = {
  name: "Aflac",
  numLegs: 2,
};

// Challenge

let dog = {
  name: "Papi",
  numLegs: 4,
};
