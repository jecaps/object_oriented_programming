(function () {
  console.log("Chirp, chirp!");
})();

/* This is an anonymus function expression that executes right awaym and outputs "Chirp, chirp!" immediately.
  Note that the function has no name and is not stored in variable.
  The two parentheses at the end of the function expression cause it to be immediately executed or invoked.
  This pattern is called immediately invoked function expression or IIFE. */

// Challenge

(function () {
  console.log("A cozy nest is ready");
})();
